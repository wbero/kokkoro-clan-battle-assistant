# Changelog

## 1.2.3 (2026-08-05)

### 修复

- **1280×720 下 `0:xx` 时钟持续低置信度**：当上一可信时间与当前低置信度主结果恰好相差 1 秒，且数字结构分数达到最低要求时，允许通过倒计时连续性校验。修复 `1:00 → 0:59` 后持续触发 `switch-clock-gate` 的问题，同时仍拒绝跳秒、时间增加和结构分数过低的结果。

### 变更

- 版本号 `1.2.2 → 1.2.3`，`versionCode 12002 → 12003`。
- 正式签名包更新为 `app-release-v1.2.3.apk`。

## 1.2.2 (2026-08-05)

### 新增

- **高频 TP 采样通道**：在原有单个 MediaProjection、单个 VirtualDisplay 架构内，将 TP 检测移到独立高频线程，减少一帧满 TP 后立即释放 UB 的漏检。
- **TP 异常诊断字段**：`energy.csv` 新增 `visualObstruction`，用于记录 BOSS 动画或 HUD 遮挡造成的不可信 TP 变化。
- **开关轴支持 BOSS 延迟 0 秒**：可直接使用提前确认的 BOSS UB 事件，行为与顺序轴即时 BOSS 触发保持一致。

### 修复

- **一帧高速充能 UB 漏检**：支持 `低 TP → 100%（仅一帧）→ 低 TP`，满 TP 只需捕获一帧；释放在连续两个低值采样后确认。
- **单帧 TP 闪烁误报 UB**：过滤 `100% → 瞬间低一帧 → 100%`，避免角色尚未释放 UB 就被错误关闭 SET。
- **多人 TP 同时异常下降**：当全队满 TP 后下一帧出现至少两人同时大幅下降时，判定为画面遮挡，不再上报多个角色 UB。
- **角色 SET 兜底误关**：TP 画面异常会清除本节点的假满 TP 基线，必须重新观察到可信满 TP 后才允许兜底确认 UB。
- **接近满 TP 的角色 UB 漏检**：角色 TP 长时间稳定在约 93%～97% 时可作为备用武装条件，解决实际已满但识别条只到约 94% 的情况；单帧接近满不会武装。
- **无效时钟帧丢失角色 UB**：角色 UB 恰好发生在时钟识别失败帧时会暂存，并在下一有效时钟帧交给开关轴。
- **同批错误候选污染后续节点**：当前角色节点成功消费同一动画中的目标角色后，会清除同批其余候选，避免误触发后续角色节点。
- **BOSS UB 被 TP 假触发永久排除**：公共 BOSS 检测器在 TP 证据被多人异常变化污染后，可继续从新的可信时停区间恢复识别；顺序轴与开关轴共用同一结果。
- **极端超宽屏控件错位**：分别校准顶部 HUD、右侧 AUTO/全体 SET、加载提示等锚点，修复 2800×800 等比例下识别框和点击位置偏移。
- **1280×720 下 0:xx 时钟持续低置信度**：当主识别结果虽然低于全局置信度阈值，但与上一可信时间恰好连续减少 1 秒且数字结构分数达到底线时，允许按倒计时连续性通过；不会放宽跨秒跳变或时间增加。

### 变更

- 版本号 `1.2.1 → 1.2.2`，`versionCode 12001 → 12002`。
- 正式签名包更新为 `app-release-v1.2.2.apk`。

## 1.2.1 (2026-08-05)

### 修复

- **Android 14+ MediaProjection 投影被立即撤销**：`recreateEnergyDisplay()` 在同一 MediaProjection 实例上第二次调用 `createVirtualDisplay()`（创建 "KokkoroEnergyCapture" 用于 TP 能量条采样），违反 Android 14+ 单实例限制，导致系统判定令牌无效并立即撤销投影。已禁用第二次虚拟显示屏创建，回退到帧内 TP 采样。
- **onStop 不再尝试复用旧令牌**：删除 `reacquireProjection()` 逻辑，投影被系统停止后直接清理资源并关闭服务。
- **防止重复令牌消费**：新增 `CaptureState` 状态机（IDLE → STARTING → ACTIVE → STOPPED），阻止重复 `ACTION_START` 再次消耗同一授权令牌。

## 1.2.0 (2026-08-04)

### 新增

- **独立卡帧悬浮窗**：与主控制面板解耦的纯悬浮窗卡帧工具，不依赖截图识别服务。
  - 三档「卡N帧」按钮，每档独立设置帧率（ms/帧）与帧数，可在 App 设置页单独配置。
  - 首次点击立即冻结游戏（焦点立刻转移至悬浮窗），再次点击前进 N 帧后重新卡住。
  - 「恢复」按钮一键释放焦点，恢复正常战斗。
  - 支持拖动、右下角把手任意缩放、点按缩放循环、最小化为小图标；位置/缩放/最小化状态独立持久化。
- **识别点击并行化**：截图捕获与识别/点击流水线解耦，识别线程与帧提交异步并行，减少帧间延迟。
- **安全暂停锁存**：战斗暂停状态锁存，避免识别间隙误判导致重复点击。
- **调试区域悬浮窗**：`DebugRegionOverlay` 在屏幕上可视化识别区域框，便于调试与校准。

### 修复

- 卡帧与 BOSS UB 交互修复：UB 触发期间卡帧状态机不再互相干扰。
- 悬浮窗按钮对齐：清零 Button 默认 `minHeight`，统一行高与垂直居中，消除各按钮高度不一致。
- 最小化按钮被右下角缩放把手遮挡：预留 34dp 固定空间。

### 变更

- 版本号 1.1.0 → 1.2.0（versionCode 11000 → 12000）。

---

## 1.1.0 (2026-07-26)

- 首个公开版本，包含实时屏幕识别、顺序轴执行、开关轴 + 卡帧、能量检测、战斗状态机、悬浮窗控制、诊断模式。
