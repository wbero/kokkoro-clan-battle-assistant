# Changelog

## 3.0.0 (2026-08-09)

### 新增

- **角色 UB 技能名识别**：轴文件支持 `角色1UB`～`角色5UB`，在 UB 浪花中直接识别游戏内技能名称，并在本场五个候选中完成角色映射；角色昵称继续只用于显示/轴内别名，不参与身份判断。
- **离线中文 OCR**：内置 ML Kit 中文文字识别模型，仅在 UB 技能框出现时异步识别，不依赖 Google Play 服务或运行时下载；新增本地视频回放 instrumentation 测试工具，可直接用实机录像验证技能名识别。
- **UB 技能名诊断日志**：`frames.csv` 新增 OCR 原文、最佳候选、最终角色、匹配分数、分差和事件时钟等字段，便于直接定位识别失败原因。
- **SET/AUTO 内部权威状态机**：完成开局初始化后，自动控制状态由内部状态机维护；每次成功下发的 SET/AUTO 点击按确定性反转更新，视觉模板降级为初始化与短期事后校验。

### 修复

- **1 帧 UB / TP 闪烁导致角色身份不稳定**：配置 `角色NUB` 后，角色 UB 身份以技能名为主证据，不再让 TP 比例、头像爆光或动画污染覆盖最终角色判断；TP/爆光继续用于辅助诊断与旧轴兼容。
- **开关轴 UB 动画内 SET 误点**：技能名确认角色后立即在 UB 动画内按内部状态计算完整 SET/AUTO 差异，不再依赖当帧 SET 模板决定“该点谁”，修复动画把 ROLE5 等按钮误读后发生反向多点的问题。
- **视觉 SET 漂移误触发安全暂停**：视觉一致性检查只在自动点击后的有限验收窗口内生效；普通战斗期间偶发或持续模板漂移不会再推翻内部状态并触发 `control-state-desync`。
- **内部状态提前从战斗转场画面初始化**：权威状态只在战斗进入可执行阶段后建立，避免加载/入场动画中的伪可信模板帧污染整场状态。
- **Accessibility 明确失败后内部状态错位**：手势被取消、拒绝、服务断开或提交超时时，会回滚对应的乐观状态反转并安全暂停；正常完成的手势继续由短期视觉窗口做事后校验。
- **开关轴 CHARACTER_UB 被后续同角色 UB 补触发**：角色 UB 事件携带采集时的游戏时钟，`1:03 | UB后=角色3` 只接受 1:03 对应的角色3 UB；错过后安全暂停，不会被更晚的同角色 UB 补掉。
- **部分实机接近满 TP 后释放漏检**：保留原有满 TP 强证据，同时新增受限的接近满 TP 慢帧兜底和窄分差头像佐证，不全局放宽 TP/爆光阈值。

### 变更

- **顺序轴严格等待真实角色 UB**：角色生命周期固定为“SET 开 → 一直等待该角色真实 UB → UB 动画内 SET 关 → 下一个角色”；删除原来的按时钟/TP 兜底超时关闭机制及对应设置项。
- **技能名模式要求五角色完整配置**：`角色1UB`～`角色5UB` 要么全部留空使用旧识别链，要么全部填写且名称唯一，避免未配置角色 UB 与 BOSS UB 混淆。
- “关于”页继续直接读取安装包 `versionName`，升级 APK 后自动显示 `3.0.0`。
- 版本号 `2.1.0 → 3.0.0`，`versionCode 20100 → 30000`。
- 正式签名包更新为 `app-release-v3.0.0.apk`。

## 2.1.0 (2026-08-08)

### 新增

- **开关轴批量控制收敛**：节点触发后一次计算完整 SET/AUTO 差异，SET 在前、AUTO 在后按点击间隔连续提交，完成后再统一确认最终目标状态，减少逐按钮等待造成的延迟。
- **开关轴批量点击诊断**：`switch.csv` 记录实际批量控制动作，`frames.csv` 补充原始 TP 候选与慢帧兜底角色，便于区分识别问题、候选歧义和点击重试。
- **慢帧 TP 释放兜底**：当高频 TP 候选不稳定时，可利用相邻慢帧的“满 TP → 低 TP”强证据补充角色 UB 候选，并优先保留与强证据一致的角色。

### 修复

- **开关轴重复点击已成功 SET**：批量控制会记住已确认成功的按钮；UB 动画短暂遮住 SET 徽标时不再立刻重复点击，只有连续多个正常可信帧确认状态确实相反后才允许重试。
- **UB 动画期间丢失点击确认**：视觉 HOLD 期间允许已经发出的 SET/AUTO 点击完成确认，但仍禁止规划新的单步点击，避免连续 UB 动画让控制状态长期停留在旧值。
- **角色 UB 候选被旧 TP 事件污染**：同一批高频 TP 事件按真实采集时间保留最新候选组，较新的单角色释放可以覆盖更早的多角色歧义，时间戳完全相同的候选仍保持歧义。
- **单帧角色爆光漏检**：放宽强爆光的单帧立即确认阈值，同时继续要求角色置信度、角色间分差和后续 UB 技能框证据，修复实机上一帧角色 UB 起点偶发漏检。
- **实机 `0:xx` 同秒低置信度反复告警**：上一可信时间与当前主结果完全相同时，允许结构分数达到安全下限的主候选保持当前秒；不会接受时间增加，也没有降低通用候选阈值。
- **`FAIL low-confidence` 时悬浮窗“下一步”误回开局**：战斗中时钟暂时无效时，预览继续使用上一有效过滤时间，不再把 `null` 时钟解释成开局状态；实际轴运行状态和触发逻辑不受影响。
- **调试悬浮框污染下一帧识别**：调试描边改为完全绘制在识别 ROI 外侧，且等待开战阶段只显示开始按钮相关区域，避免 MediaProjection 把调试框本身录入下一帧模板匹配。
- **刘海/挖孔设备调试框整体偏移**：调试区域按完整显示屏坐标映射到 Overlay 本地坐标，修复系统安全区缩进造成的识别框压缩和错位。

### 变更

- 版本号 `2.0.0 → 2.1.0`，`versionCode 20000 → 20100`。
- 正式签名包更新为 `app-release-v2.1.0.apk`。

## 2.0.0 (2026-08-07)

### 新增

- **角色 UB 多证据识别链路**：新增角色头像爆光识别，并与 TP 释放候选、UB 技能名浪花框联合确认，降低单一 TP 识别在强光、时停和 BOSS UB 动画中的误判。
- **整场实战 UB 回归测试**：基于完整战斗录像固化 10 次真实角色 UB 与非角色技能框负样本，要求最终结果严格做到 0 漏识别、0 多识别、0 错角色。
- **无障碍点击状态诊断**：补充服务在线状态、手势提交/完成/取消/拒绝/超时等结果，便于区分识别故障与模拟器触摸注入故障。

### 修复

- **1 帧满 TP 后立即释放 UB 漏检**：允许满 TP 只存在单帧，并改进高频 TP 采样与完整识别之间的时间边界，避免候选与浪花错帧。
- **BOSS UB 导致 TP 抖动后误暂停**：轴尚未发出实际操作时，TP/SET/AUTO 瞬时异常只进入 HOLD，不再因为识别抖动主动打开菜单暂停；实际点击确认失败仍会安全暂停。
- **角色 UB 爆光扩散导致错角色**：提高角色爆光入口置信度，过滤普通技能、BOSS 动画和爆光尾部造成的弱角色候选，避免同一次光效从一个角色漂移到另一个角色。
- **同一次 UB 浪花被拆段后重复确认**：同一角色 UB 只允许被当前动画周期消费一次，避免重复推进开关轴。
- **非角色技能框误报角色 UB**：没有可信角色爆光或 TP 证据时，即使检测到 UB 技能名背景框也不会确认角色事件。
- **开局控制收敛过早**：防御性延迟从 1:30 调整到 1:29，减少刚进入战斗时控制状态尚未稳定导致的误操作。

### 变更

- 版本号 `1.2.3 → 2.0.0`，`versionCode 12003 → 20000`。
- “关于”页继续直接读取安装包 `versionName`，与 APK 版本号保持自动同步。
- 正式签名包更新为 `app-release-v2.0.0.apk`。

## 1.2.3 (2026-08-05)

### 修复

- **1280×720 下 `0:xx` 时钟持续低置信度**：当上一可信时间与当前低置信度主结果恰好相差 1 秒，且数字结构分数达到最低要求时，允许通过倒计时连续性校验。修复 `1:00 → 0:59` 后持续触发 `switch-clock-gate` 的问题，同时仍拒绝跳秒、时间增加和结构分数过低的结果。

### 变更

- 版本号 `1.2.2 → 1.2.3`，`versionCode 12002 → 12003`。
- 正式签名包更新为 `app-release-v1.2.3.apk`。

## 1.2.2 (2026-08-05)

### 新增

- **高频 TP 采样通道**：在原有单个 MediaProjection、单个 VirtualDisplay 架构内，将 TP 检测移到独立高频线程，减少一帧满 TP 后立即释放 UB 的漏检。
- **TP 异常诊断字段**：`energy.csv` 新增 `visualObstruction`，用于记录 BOSS 动画或 HUD 遮挡造成的不可信 TP 变化。
- **开关轴支持 BOSS 延迟 0 秒**：可直接使用提前确认的 BOSS UB 事件，行为与顺序轴即时 BOSS 触发保持一致。

### 修复

- **一帧高速充能 UB 漏检**：支持 `低 TP → 100%（仅一帧）→ 低 TP`，满 TP 只需捕获一帧；释放在连续两个低值采样后确认。
- **单帧 TP 闪烁误报 UB**：过滤 `100% → 瞬间低一帧 → 100%`，避免角色尚未释放 UB 就被错误关闭 SET。
- **多人 TP 同时异常下降**：当全队满 TP 后下一帧出现至少两人同时大幅下降时，判定为画面遮挡，不再上报多个角色 UB。
- **角色 SET 兜底误关**：TP 画面异常会清除本节点的假满 TP 基线，必须重新观察到可信满 TP 后才允许兜底确认 UB。
- **接近满 TP 的角色 UB 漏检**：角色 TP 长时间稳定在约 93%～97% 时可作为备用武装条件，解决实际已满但识别条只到约 94% 的情况；单帧接近满不会武装。
- **无效时钟帧丢失角色 UB**：角色 UB 恰好发生在时钟识别失败帧时会暂存，并在下一有效时钟帧交给开关轴。
- **同批错误候选污染后续节点**：当前角色节点成功消费同一动画中的目标角色后，会清除同批其余候选，避免误触发后续角色节点。
- **BOSS UB 被 TP 假触发永久排除**：公共 BOSS 检测器在 TP 证据被多人异常变化污染后，可继续从新的可信时停区间恢复识别；顺序轴与开关轴共用同一结果。
- **极端超宽屏控件错位**：分别校准顶部 HUD、右侧 AUTO/全体 SET、加载提示等锚点，修复 2800×800 等比例下识别框和点击位置偏移。
- **1280×720 下 0:xx 时钟持续低置信度**：当主识别结果虽然低于全局置信度阈值，但与上一可信时间恰好连续减少 1 秒且数字结构分数达到底线时，允许按倒计时连续性通过；不会放宽跨秒跳变或时间增加。

### 变更

- 版本号 `1.2.1 → 1.2.2`，`versionCode 12001 → 12002`。
- 正式签名包更新为 `app-release-v1.2.2.apk`。

## 1.2.1 (2026-08-05)

### 修复

- **Android 14+ MediaProjection 投影被立即撤销**：`recreateEnergyDisplay()` 在同一 MediaProjection 实例上第二次调用 `createVirtualDisplay()`（创建 "KokkoroEnergyCapture" 用于 TP 能量条采样），违反 Android 14+ 单实例限制，导致系统判定令牌无效并立即撤销投影。已禁用第二次虚拟显示屏创建，回退到帧内 TP 采样。
- **onStop 不再尝试复用旧令牌**：删除 `reacquireProjection()` 逻辑，投影被系统停止后直接清理资源并关闭服务。
- **防止重复令牌消费**：新增 `CaptureState` 状态机（IDLE → STARTING → ACTIVE → STOPPED），阻止重复 `ACTION_START` 再次消耗同一授权令牌。

## 1.2.0 (2026-08-04)

### 新增

- **独立卡帧悬浮窗**：与主控制面板解耦的纯悬浮窗卡帧工具，不依赖截图识别服务。
  - 三档「卡N帧」按钮，每档独立设置帧率（ms/帧）与帧数，可在 App 设置页单独配置。
  - 首次点击立即冻结游戏（焦点立刻转移至悬浮窗），再次点击前进 N 帧后重新卡住。
  - 「恢复」按钮一键释放焦点，恢复正常战斗。
  - 支持拖动、右下角把手任意缩放、点按缩放循环、最小化为小图标；位置/缩放/最小化状态独立持久化。
- **识别点击并行化**：截图捕获与识别/点击流水线解耦，识别线程与帧提交异步并行，减少帧间延迟。
- **安全暂停锁存**：战斗暂停状态锁存，避免识别间隙误判导致重复点击。
- **调试区域悬浮窗**：`DebugRegionOverlay` 在屏幕上可视化识别区域框，便于调试与校准。

### 修复

- 卡帧与 BOSS UB 交互修复：UB 触发期间卡帧状态机不再互相干扰。
- 悬浮窗按钮对齐：清零 Button 默认 `minHeight`，统一行高与垂直居中，消除各按钮高度不一致。
- 最小化按钮被右下角缩放把手遮挡：预留 34dp 固定空间。

### 变更

- 版本号 1.1.0 → 1.2.0（versionCode 11000 → 12000）。

---

## 1.1.0 (2026-07-26)

- 首个公开版本，包含实时屏幕识别、顺序轴执行、开关轴 + 卡帧、能量检测、战斗状态机、悬浮窗控制、诊断模式。
